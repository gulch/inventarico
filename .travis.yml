language: php

php:
  - 7.1
  - 7.2

before_script:
  - composer self-update
  - composer install
  - mysql -e 'create database Inventarico_Test;'
  - cp .env.travis .env
  - php artisan key:generate
  - php artisan migrate

script: phpunit